name: Week 4 Iris CI

on:
  push:
  pull_request:

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  test-iris-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest dvc dvc-gs scikit-learn pandas joblib google-cloud-bigquery 'feast[gcp]'

      - name: Configure GCP credentials
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Pull DVC artifacts
        run: dvc pull

      - name: Run pytest
        run: |
          mkdir -p reports
          set +e
          pytest tests --junitxml=reports/pytest-results.xml | tee reports/pytest-output.txt
          echo "${PIPESTATUS[0]}" > reports/pytest-exit-code.txt
          set -e

      - name: Generate model evaluation report
        if: always()
        run: python scripts/generate_cml_report.py

      - name: Run combined DVC and Feast validation
        if: always()
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          BIGQUERY_DATASET: iris_feast_week4
          BIGQUERY_TABLE: iris_features
          BIGQUERY_LOCATION: US
          INFERENCE_SAMPLE_IDS: 0,10,20
        run: python scripts/dvc_feast_validation_pipeline.py

      - name: Publish CML report
        if: always() && github.event_name == 'pull_request'
        uses: iterative/setup-cml@v2

      - name: Comment CML report
        if: always() && github.event_name == 'pull_request'
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          {
            echo "## Pytest Output"
            echo '```'
            cat reports/pytest-output.txt
            echo '```'
            echo
            cat reports/evaluation_report.md
            echo
            cat reports/dvc_feast_validation_report.md
          } > reports/cml-report.md
          cml comment create reports/cml-report.md

      - name: Fail if pytest failed
        if: always()
        run: exit "$(cat reports/pytest-exit-code.txt)"
